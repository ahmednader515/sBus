<% layout('layouts/boilerplate') %>

<body class="d-flex flex-column vh-100">

<div class="service-text"></div>

    <h1>لوحة تحكم مالك</h1>

</div>

<a href="/frontEvents">calendar front</a>
<a href="/backEvents">calendar back</a>

</body>

<!-- <div class="service-text">

    <h1>لوحة التحكم</h1>

    <h2>خدماتك</h2>

</div>

<div class="service-container">

    <% for(let fullService of fullServices) { %>

        <% if(currentUser && fullService.author.equals(currentUser._id)) { %>
    
            <div class="card" mb-3>
                <div class="row">
                    <div class="col-md-4">
                        <img class="img-fluid service-img" alt="" src="<%=fullService.image%>">
                    </div>
                    <div class="col-md-8">
                        <div class="card-body">
                            <h5 class="card-title"><%= fullService.title %> </h5>
                            <p class="card-text"><%= fullService.description %></p>
                            <p class="card-text">
                                <small class="text-muted"><%= fullService.location%></small>
                            </p>
                            <a class="btn btn-primary" href="/fullServices/<%=fullService._id%>">المزيد</a>
                        </div>
                    </div>
                </div>
            </div>
    
        <% } %>
    
    <% } %>

    <% for(let halfService of halfServices) { %>

        <% if(currentUser && halfService.author.equals(currentUser._id)) { %>
    
            <div class="card" mb-3>
                <div class="row">
                    <div class="col-md-4">
                        <img class="img-fluid service-img" alt="" src="<%=halfService.image%>">
                    </div>
                    <div class="col-md-8">
                        <div class="card-body">
                            <h5 class="card-title"><%= halfService.title %> </h5>
                            <p class="card-text"><%= halfService.description %></p>
                            <p class="card-text">
                                <small class="text-muted"><%= halfService.location%></small>
                            </p>
                            <a class="btn btn-primary" href="/halfServices/<%=halfService._id%>">المزيد</a>
                        </div>
                    </div>
                </div>
            </div>
    
        <% } %>
    
    <% } %>
    
    <div class="button-container">

        <button class="btn"><a href="/services/choose">اضافة خدمة جديدة</a></button>

    </div>

</div>


<div class="service-text">

    <h2>طرودك</h2>

</div>

<div class="service-container"></div>

    <% for(let package of packages) { %>

        <% if(currentUser && package.author.equals(currentUser._id)) { %>
    
            <div class="card" mb-3>
                <div class="row">
                    <div class="col-md-4">
                        <img class="img-fluid service-img" alt="" src="<%=package.image%>">
                    </div>
                    <div class="col-md-8">
                        <div class="card-body">
                            <h5 class="card-title"><%= package.title %> </h5>
                            <p class="card-text"><%= package.description %></p>
                            <p class="card-text">
                                <small class="text-muted"><%= package.location%></small>
                            </p>
                            <a class="btn btn-primary" href="/packages/<%=package._id%>">المزيد</a>
                        </div>
                    </div>
                </div>
            </div>
    
        <% } %>
    
    <% } %>
    
    <div class="button-container">

        <button class="btn"><a href="/packages/new">اضافة طرد جديدة</a></button>

    </div>

</div> -->