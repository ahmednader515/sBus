<% layout('layouts/boilerplate') %>

<div class="row">
    <div class="col">
        <div class="card mb-3">
            <!-- package Image -->
            <img src="<%= halfService.image %>" class="card-img-top" alt="Service Image">

            <!-- Card Body with Title and Description -->
            <div class="card-body">
                <h5 class="card-title">العنوان: <%= halfService.title %></h5>
                <p class="card-text">الوصف: <%= halfService.description %></p>
            </div>

            <!-- package Details -->
            <ul class="list-group list-group-flush">
                <li class="list-group-item text-muted">الموقع: <%= halfService.location %></li>
                <li class="list-group-item">السعر: $<%= halfService.price %></li>
            </ul>

            <!-- User Actions (Edit/Delete or Buy) -->
            <div class="card-body">
                <% if (currentUser && halfService.author.equals(currentUser._id)) { %>
                    <!-- Edit and Delete Options -->
                    <a href="/halfServices/<%= halfService._id %>/edit" class="btn btn-info">تعديل</a>
                    <form class="d-inline" action="/halfServices/<%= halfService._id %>?_method=DELETE" method="POST">
                        <button class="btn btn-danger">مسح</button>
                    </form>
                <% } else { %>
                    <a class="btn" href="/halfServices/<%= halfService._id %>/order">طلب</a>
                <% } %>
            </div>
        </div>
    </div>
</div>
