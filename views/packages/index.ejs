<% layout('layouts/boilerplate') %>

<div class="service-text">
    <h1>الطرود</h1>
</div>

<div class="service-container">
    <% packages.forEach(package => { %>
        <div class="card service mb-3">
            <div class="row g-0">
                <!-- Image Column -->
                <div class="col-md-4">
                    <img class="img-fluid service-img" alt="Service Image" src="<%= package.image %>">
                </div>
                <!-- Details Column -->
                <div class="col-md-8">
                    <div class="card-body">
                        <h5 class="card-title"><%= package.title %></h5>
                        <p class="card-text"><%= package.description %></p>
                        <p class="card-text">
                            <small class="text-muted"><%= package.location %></small>
                        </p>
                        <a class="btn btn-primary" href="/packages/<%= package._id %>">المزيد</a>
                    </div>
                </div>
            </div>
        </div>
    <% }) %>
</div>
