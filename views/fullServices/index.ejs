<% layout('layouts/boilerplate') %>

<div class="service-text">
    <h1>الخدمات</h1>
</div>

<div class="service-container">
    <% fullServices.forEach(fullService => { %>
        <div class="card service mb-3">
            <div class="row g-0">
                <!-- Image Column -->
                <div class="col-md-4">
                    <img class="img-fluid service-img" alt="Service Image" src="<%= fullService.image %>">
                </div>
                <!-- Details Column -->
                <div class="col-md-8">
                    <div class="card-body">
                        <h5 class="card-title"><%= fullService.title %></h5>
                        <p class="card-text"><%= fullService.description %></p>
                        <p class="card-text">
                            <small class="text-muted"><%= fullService.location %></small>
                        </p>
                        <a class="btn btn-primary" href="/fullServices/<%= fullService._id %>">المزيد</a>
                    </div>
                </div>
            </div>
        </div>
    <% }) %>
</div>
